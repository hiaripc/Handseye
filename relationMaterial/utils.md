# A summary of commands and utility:

Yolo 5 detect command from yolov5 folder:
```
python detect.py --source [path|number of webcam (see /dev/video)] --weights [path to .pt file] --conf [confidence threshold] 
```